import {Button,Paper,Stack,Typography} from '@mui/material';import api from '../services/api';
export default function Reports(){const download=async format=>{const r=await api.get(`/reports/export/${format}/`,{responseType:'blob'});const url=URL.createObjectURL(r.data);const a=document.createElement('a');a.href=url;a.download=`finance-report.${format==='excel'?'xlsx':format}`;a.click();URL.revokeObjectURL(url)};return <><Typography variant="h4" gutterBottom>Reports</Typography><Paper sx={{p:3}}><Typography color="text.secondary" sx={{mb:3}}>Download all of your transactions in a professional report format.</Typography><Stack direction={{xs:'column',sm:'row'}} spacing={2}><Button variant="contained" onClick={()=>download('pdf')}>Download PDF</Button><Button variant="outlined" onClick={()=>download('csv')}>Download CSV</Button><Button variant="outlined" onClick={()=>download('excel')}>Download Excel</Button></Stack></Paper></>}
